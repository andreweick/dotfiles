{{/* ~/.chezmoiignore.tmpl */}}

# This file is wierd -- think of it like I ignore the files from the Source tree to the destination tree **BUT**
# the file names are post decroated.  In the source tree, the secrets.conf file is *really* named encrypted_private_secrets.conf.age,
# but the chezmoi machineray adds encrypted_private and appends .age to the file name

README.md
install.sh

# Makeing a non-standard extension (.age-symmetric) so that chezmoi machinery doesn't try to decrypt it with the key material
private_dot_config/age/secret_key.txt.age-symmetric
private_dot_config/rclone/secrets.conf

{{- $keyPath := joinPath .chezmoi.homeDir ".config" "age" "key.txt" -}}
{{- if not (stat $keyPath) -}}
# Age key not found, so ignore the encrypted rclone config
private_dot_config/rclone/encrypted_private_rclone.conf.age
{{- end -}}
