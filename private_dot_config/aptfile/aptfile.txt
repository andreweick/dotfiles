# APT Package List — base system layer only.
#
# mise's global tool list is now empty and its Linux auto-install scripts are
# gone, so this is Linux's only automated package source: the shell, git,
# ssh, and the toolchain age needs to run.
#
# Lines starting with # are comments and will be skipped.

zsh
git
openssh-client
curl
ca-certificates
build-essential
