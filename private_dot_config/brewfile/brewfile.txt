# Homebrew owns the general CLI tool layer on macOS now — mise's global
# [tools] list is empty (see ~/.config/mise/config.toml). Per-project tool
# pins still live in each project's own mise.toml (e.g. spouterinn, edc);
# mise itself stays installed here so those still work.
#
# Linux has no equivalent list yet — these tools aren't installed there by
# this repo (see aptfile.txt).

brew "mise"
brew "chezmoi"
brew "atuin", restart_service: :changed
brew "age"
brew "cosign"
brew "gh"
brew "rclone"
brew "starship"
brew "node"
brew "just"
brew "zoxide"
brew "ripgrep"
brew "fzf"
brew "gum"
brew "bat"
brew "jless"
