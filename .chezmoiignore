# README.md
install.sh
key.txt.age

# Chezimoiignore is treated as a template regardless of extension

# If the file ~/.config/age/key.txt **DOES NOT** exist, 
# ignore the enclosed files
{{ if (not (stat (joinPath .chezmoi.homeDir ".config" "age" "key.txt"))) }}
*/rclone.conf
 
{{ end }}
